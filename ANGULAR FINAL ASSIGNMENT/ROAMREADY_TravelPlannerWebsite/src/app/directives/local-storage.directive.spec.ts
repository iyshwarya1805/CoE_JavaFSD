import { LocalStorageDirective } from './local-storage.directive';

describe('LocalStorageDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalStorageDirective();
    expect(directive).toBeTruthy();
  });
});
