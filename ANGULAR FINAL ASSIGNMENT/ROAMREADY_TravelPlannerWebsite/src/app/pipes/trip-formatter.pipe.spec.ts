import { TripFormatterPipe } from './trip-formatter.pipe';

describe('TripFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TripFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
